*.iml
.gradle/
build/
local.properties
.idea/
.DS_Store
/captures
.externalNativeBuild
.cxx
build_output.txt

# El PIN embebido está publicado a pedido del dueño del dispositivo (ver README).
# No se ignora: pin-local.properties se versiona a propósito.

# Salida local del build privado (artifact de trabajo).
dist-privado/
