# PIN embebido del dispositivo (PUBLICADO a pedido del dueño, ver README).
#
# Este archivo SÍ se versiona: el dueño del equipo autorizó explícitamente publicar
# el PIN del Note10 viejo (teléfono físico de pantalla rota, PIN que no se reutiliza
# en ningún otro servicio). Un clon del repo compila por defecto el APK CON el PIN.
#
# Para compilar el APK PÚBLICO (sin PIN):  ./gradlew assembleDebug -PskipPin=true
pin=8628
